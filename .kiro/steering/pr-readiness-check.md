---
inclusion: auto
---

# Проверка готовности ветки к Pull Request

## Когда использовать

Запускайте эту проверку когда:
- Пользователь просит "проверить ветку на готовность к PR"
- Пользователь говорит "подготовь PR"
- Пользователь спрашивает "готова ли ветка"
- Завершена работа над feature/bugfix веткой

## Автоматический процесс проверки

### Шаг 1: Запуск всех проверок CI/CD

**ОБЯЗАТЕЛЬНО запустить ВСЕ проверки в указанном порядке:**

#### 1.1 Frontend проверки

```bash
cd frontend

# Type check
npm run type-check

# Linting
npm run lint

# Formatting check
npm run format:check

# Tests (БЕЗ watch mode!)
npm test

# Build
npm run build
```

**Критерий успеха:** Все команды завершаются с exit code 0, все тесты проходят.

#### 1.2 Backend проверки

```bash
cd backend

# Ruff linting
ruff check .

# Black formatting check
black --check .

# Tests
pytest
```

**Критерий успеха:** Все команды завершаются с exit code 0, все тесты проходят.

### Шаг 2: Обработка ошибок

**ЕСЛИ хотя бы одна проверка не прошла:**

1. **Анализируй ошибку:**
   - Читай вывод команды полностью
   - Определи причину (синтаксис, типы, тесты, форматирование)

2. **Исправляй автоматически:**
   - Для форматирования: `npm run format` или `black .`
   - Для линтинга: исправь код согласно ошибкам
   - Для тестов: исправь код или тесты
   - Для типов: исправь типизацию

3. **Повторяй проверку:**
   - Запусти ту же команду снова
   - Если ошибка осталась - анализируй глубже
   - Продолжай пока ВСЕ проверки не пройдут

**КРИТИЧНО:** НЕ переходи к следующему шагу пока ВСЕ проверки не пройдут успешно!

### Шаг 3: Подготовка CHANGELOG (БЕЗ коммита!)

**ВАЖНО: CHANGELOG готовится БЕЗ коммита, только для показа пользователю!**

#### 3.1 Анализ изменений в ветке

```bash
# Получить список коммитов в ветке
git log main..HEAD --oneline

# Получить статистику изменений
git diff main..HEAD --stat
```

#### 3.2 Формат записи CHANGELOG

```markdown
## X.X.X - ГГГГ-ММ-ДД

### Ключевые изменения (Краткое описание области)

- **Категория:** описание изменений через точку с запятой; еще изменения; и еще.
- **Категория:** описание изменений через точку с запятой; еще изменения.
```

**Стиль описания:**
- Компактный формат (как в версии 0.0.9)
- Одна строка на категорию
- Пункты разделены точкой с запятой
- Группировка по категориям (Frontend, Backend, Тесты, Документация и т.д.)
- Только ключевые изменения, без деталей

**Пример:**
```markdown
## 0.1.0 - 2026-02-23

### Ключевые изменения (Темная тема, Редизайн UI, Исправления)

- **Темная тема:** полный редизайн интерфейса с современной темной цветовой схемой (токены из globals.css: bg-background #111827, bg-card #1F2933, border-border #374151); применена на всех страницах (главная, dashboard, транзакции, категории, бюджеты, recurring); обновлены все компоненты для использования токенов цветов.
- **Иконки категорий:** замена эмодзи на lucide-react иконки; новые компоненты CategoryIcon и CategorySelect; добавлены иконки в таблицу топ категорий на dashboard; обновлены формы для использования новых иконок.
```

#### 3.3 Показать CHANGELOG пользователю

**НЕ коммитить CHANGELOG автоматически!**

Вместо этого:
1. Покажи подготовленный текст CHANGELOG пользователю
2. Спроси: "Одобряете ли вы этот CHANGELOG?"
3. Дождись подтверждения
4. Только после подтверждения - обнови файл и закоммить

### Шаг 4: Подготовка описания PR

**Формат описания PR:**

```markdown
## 🎯 [Краткое название из CHANGELOG]

### Ключевые изменения

[Скопировать из CHANGELOG, добавить эмодзи]

**✅ [Категория]**
- Пункт 1
- Пункт 2

**🐛 [Исправления]** (если есть)
- Проблема
- Решение

### Результаты тестов
```
Test Suites: X passed, X total
Tests:       X passed, X total
```

### Как проверить
```bash
# Команды для локальной проверки
docker-compose up -d
# Открыть http://localhost:3001
```
```

**Пример:**

```markdown
## 🎯 Темная тема и редизайн интерфейса

### Ключевые изменения

**✅ Темная тема**
- Полный редизайн интерфейса с современной темной цветовой схемой
- Применена на всех страницах (главная, dashboard, транзакции, категории, бюджеты, recurring)
- Обновлены все компоненты для использования токенов цветов из globals.css

**✅ Иконки категорий**
- Замена эмодзи на lucide-react иконки
- Новые компоненты CategoryIcon и CategorySelect
- Добавлены иконки в таблицу топ категорий на dashboard

**✅ Интеграционные тесты**
- Добавлено 15 новых тестов для страницы recurring
- Исправлены проблемы с pointer-events и таймаутами

**🐛 Исправления**
- Пагинация транзакций: исправлен подсчет total в backend
- Оптимизация запросов: уменьшено количество запросов при загрузке страницы с 3 до 1
- Форма recurring: исправлена ошибка getCategoryIcon, валюта по умолчанию RUB

### Результаты тестов
```
Test Suites: 18 passed, 18 total
Tests:       83 passed, 83 total
```

### Как проверить
```bash
# Запустить проект
docker-compose up -d

# Открыть в браузере
http://localhost:3001

# Проверить темную тему на всех страницах
# Проверить иконки категорий
# Проверить работу форм и пагинации
```
```

### Шаг 5: Финальный отчет

**После успешного прохождения всех проверок, выведи:**

```
✅ Ветка готова к Pull Request!

📋 Результаты проверок:
- Frontend type-check: ✅ Passed
- Frontend lint: ✅ Passed  
- Frontend format: ✅ Passed
- Frontend tests: ✅ 83 passed
- Frontend build: ✅ Passed
- Backend ruff: ✅ Passed
- Backend black: ✅ Passed
- Backend tests: ✅ Passed

📝 CHANGELOG подготовлен (требует одобрения)

📄 Описание PR подготовлено

Следующие шаги:
1. Одобрите CHANGELOG
2. Я закоммичу CHANGELOG
3. Создайте PR с подготовленным описанием
```

## Правила поведения

### ✅ ДЕЛАЙ:

1. **Запускай ВСЕ проверки** - не пропускай ни одну
2. **Исправляй ошибки автоматически** - не спрашивай разрешения на очевидные исправления
3. **Повторяй проверки** - пока все не пройдут
4. **Готовь CHANGELOG БЕЗ коммита** - только показывай пользователю
5. **Жди одобрения CHANGELOG** - не коммить без подтверждения
6. **Готовь описание PR** - в правильном формате
7. **Выводи финальный отчет** - с результатами всех проверок

### ❌ НЕ ДЕЛАЙ:

1. **НЕ пропускай проверки** - даже если кажется что все ок
2. **НЕ коммить CHANGELOG автоматически** - только после одобрения
3. **НЕ переходи к следующему шагу** - если есть ошибки
4. **НЕ спрашивай разрешения** - на исправление очевидных ошибок (форматирование, линтинг)
5. **НЕ создавай PR автоматически** - только готовь описание
6. **НЕ используй verbose summaries** - будь лаконичен

## Примеры использования

### Пример 1: Все проверки прошли с первого раза

```
Пользователь: "проверь ветку на готовность к PR"

Kiro:
1. Запускаю frontend проверки...
   ✅ Type check passed
   ✅ Lint passed
   ✅ Format check passed
   ✅ Tests passed (83/83)
   ✅ Build passed

2. Запускаю backend проверки...
   ✅ Ruff passed
   ✅ Black passed
   ✅ Tests passed

3. Готовлю CHANGELOG...

## 0.1.0 - 2026-02-23

### Ключевые изменения (Темная тема, Редизайн UI)

- **Темная тема:** полный редизайн интерфейса...
- **Иконки категорий:** замена эмодзи на lucide-react...

Одобряете ли вы этот CHANGELOG?
```

### Пример 2: Есть ошибки форматирования

```
Пользователь: "проверь ветку на готовность к PR"

Kiro:
1. Запускаю frontend проверки...
   ✅ Type check passed
   ✅ Lint passed
   ❌ Format check failed - найдены проблемы форматирования

   Исправляю автоматически...
   > npm run format
   
   Повторяю проверку...
   ✅ Format check passed
   
   ✅ Tests passed (83/83)
   ✅ Build passed

2. Запускаю backend проверки...
   [продолжение...]
```

### Пример 3: Падают тесты

```
Пользователь: "проверь ветку на готовность к PR"

Kiro:
1. Запускаю frontend проверки...
   ✅ Type check passed
   ✅ Lint passed
   ✅ Format check passed
   ❌ Tests failed (82/83)
   
   Анализирую ошибку...
   Тест "TransactionFilters - does not send empty type" падает
   Причина: изменилось поведение компонента
   
   Исправляю тест...
   [показать изменения]
   
   Повторяю проверку...
   ✅ Tests passed (83/83)
   
   ✅ Build passed

2. Запускаю backend проверки...
   [продолжение...]
```

## Интеграция с существующими правилами

Этот процесс дополняет:
- `commit-guidelines.md` - используй правила коммитов при исправлении ошибок
- `pr-guidelines.md` - используй формат PR при подготовке описания

## Автоматизация через команду

Пользователь может использовать любую из этих фраз:
- "проверь ветку на готовность к PR"
- "подготовь PR"
- "готова ли ветка к PR"
- "запусти проверки перед PR"
- "проверь готовность к Pull Request"

При любой из этих фраз - запускай полный процесс проверки автоматически.
