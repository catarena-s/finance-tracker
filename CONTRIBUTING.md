# Руководство по внесению изменений

## 🌍 Язык

**Весь код, коммиты, PR и документация должны быть на русском языке.**

## 📝 Процесс разработки

### 1. Подготовка к новому этапу

**Перед началом работы над новым этапом/фичей:**

```bash
# 1. Переключиться на main
git checkout main

# 2. Актуализировать main из upstream (original)
git fetch upstream
git merge upstream/main

# 3. Отправить изменения в свой fork
git push origin main

# 4. Создать новую ветку для разработки
git checkout -b feature/название-этапа
```

**Важно:** Всегда начинайте новый этап с актуальной ветки main!

### 2. Создание ветки

**Формат имени ветки:**
```bash
feature/название-фичи    # Для новых функций
fix/название-бага        # Для исправлений
refactor/название        # Для рефакторинга
docs/название            # Для документации
```

**Примеры:**
```bash
git checkout -b feature/stage3-frontend-implementation
git checkout -b fix/dashboard-validation-error
```

### 3. Разработка

- Следуйте структуре проекта
- Пишите тесты для нового кода
- Обновляйте документацию при необходимости

### 4. Коммиты

**Правила:**
- Все коммиты на русском языке
- Один коммит = одна завершенная задача
- Формат: `<тип>(<область>): <описание>`

**Примеры:**
```bash
feat(frontend): добавить интеграционные тесты
fix(backend): исправить ошибку валидации
test(frontend): добавить тесты для форм
```

**Когда делать коммит:**
- ✅ Задача завершена
- ✅ Все тесты проходят
- ✅ Код скомпилирован
- ❌ Код не работает
- ❌ Тесты падают

Подробнее: [Правила коммитов](.kiro/steering/commit-guidelines.md)

### 5. Pull Request

**Правила:**
- Описание на русском языке
- Лаконичное описание (только ключевые изменения)
- Не расписывать подробно все файлы

**Структура:**
```markdown
## 🎯 Название

### Ключевые изменения
- Что добавлено/изменено
- Что исправлено

### Результаты тестов
### Как проверить
```

Подробнее: [Правила PR](.kiro/steering/pr-guidelines.md)

## 🧪 Тестирование

### Перед коммитом

```bash
# Frontend
cd frontend
npm run type-check
npm test
npm run lint

# Backend
cd backend
pytest
ruff check .
mypy .
```

### Типы тестов

- **Unit тесты** - для отдельных функций/компонентов
- **Integration тесты** - для пользовательских сценариев
- **Property-based тесты** - для проверки универсальных свойств

## 📁 Структура проекта

```
finance_tracker/
├── backend/          # FastAPI backend
│   ├── app/
│   ├── tests/
│   └── alembic/
├── frontend/         # Next.js frontend
│   ├── src/
│   ├── __tests__/
│   └── public/
└── .kiro/
    ├── specs/        # Спецификации фич
    └── steering/     # Правила и гайдлайны
```

## 🔧 Настройка окружения

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### Frontend

```bash
cd frontend
npm install
```

## 📚 Дополнительные ресурсы

- [Правила коммитов](.kiro/steering/commit-guidelines.md)
- [Правила PR](.kiro/steering/pr-guidelines.md)
- [Архитектура проекта](ARCHITECTURE.md)

## ❓ Вопросы

Если у вас есть вопросы, создайте issue или обратитесь к команде.

## 📄 Лицензия

Этот проект разработан в рамках учебного курса.
