# Руководство по тестированию фронтенда

## Внесенные исправления

### 1. API клиенты (services/api/)

Исправлены все API клиенты для работы с реальным форматом ответов бэкенда:

#### categories.ts
- ✅ Убрано `response.data.data`, теперь `response.data`
- ✅ Бэкенд возвращает массив напрямую

#### transactions.ts
- ✅ Добавлена трансформация формата пагинации
- ✅ Бэкенд: `{items, total, page, page_size, pages}`
- ✅ Фронтенд: `{data, pagination: {page, pageSize, totalPages, totalItems}}`

#### budgets.ts
- ✅ Убрано `response.data.data`, теперь `response.data`
- ✅ Бэкенд возвращает массив напрямую

#### analytics.ts
- ✅ Убрано `response.data.data`, теперь `response.data`
- ✅ Бэкенд возвращает объект напрямую

### 2. API клиент (services/api/client.ts)

Добавлено автоматическое преобразование форматов:

- ✅ **Request interceptor**: преобразует camelCase → snake_case
- ✅ **Response interceptor**: преобразует snake_case → camelCase

**Примеры преобразования:**
```
categoryId → category_id
transactionDate → transaction_date
createdAt → created_at
```

## Как проверить

### 1. Откройте браузер

Перейдите на: http://localhost:3000

### 2. Проверьте дашборд

Должны отображаться:
- ✅ Общий баланс
- ✅ Доходы за период
- ✅ Расходы за период
- ✅ Количество транзакций

### 3. Проверьте страницу транзакций

Перейдите на: http://localhost:3000/transactions

Должны отображаться:
- ✅ Список транзакций
- ✅ Фильтры (по типу, категории, дате)
- ✅ Пагинация
- ✅ Кнопка создания новой транзакции

### 4. Проверьте страницу категорий

Перейдите на: http://localhost:3000/categories

Должны отображаться:
- ✅ Список категорий с иконками
- ✅ Цвета категорий
- ✅ Типы (доход/расход)

### 5. Проверьте страницу бюджетов

Перейдите на: http://localhost:3000/budgets

Должны отображаться:
- ✅ Список бюджетов
- ✅ Прогресс выполнения
- ✅ Оставшиеся суммы

### 6. Проверьте консоль браузера

Откройте DevTools (F12) и проверьте:
- ❌ Не должно быть ошибок в консоли
- ✅ Запросы к API должны возвращать 200 OK
- ✅ Данные должны корректно преобразовываться

## Тестовые данные

В системе уже есть тестовые данные:

### Категории (14 шт.)
- Продукты 🛒 (расход)
- Транспорт 🚗 (расход)
- Зарплата 💰 (доход)
- И другие...

### Транзакции (14 шт.)
- Зарплата за февраль: 50000 ₽ (доход)
- Покупка продуктов: 1500 ₽ (расход)
- Кофе в кафе: 800 ₽ (расход)
- Ужин в ресторане: 3000 ₽ (расход)
- И другие...

### Бюджеты
- Продукты: 15000 ₽ на месяц

## Если данные не отображаются

### 1. Проверьте консоль браузера

Откройте DevTools (F12) → Console

Ищите ошибки типа:
- `TypeError: Cannot read property 'data' of undefined`
- `Network Error`
- `CORS Error`

### 2. Проверьте Network tab

Откройте DevTools (F12) → Network

Проверьте запросы к API:
- Статус должен быть 200 OK
- Response должен содержать данные
- Headers должны включать CORS заголовки

### 3. Проверьте, что бэкенд работает

Откройте в браузере: http://127.0.0.1:8000/api/v1/categories/

Должен вернуться JSON с категориями.

### 4. Проверьте CORS

Если видите ошибку CORS, проверьте файл `backend/.env`:
```
CORS_ORIGINS=http://localhost:3000,http://localhost:8000
```

### 5. Перезапустите сервисы

Если ничего не помогает:

```bash
# Остановите все процессы
# Ctrl+C в терминалах

# Перезапустите бэкенд
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Перезапустите фронтенд (в другом терминале)
cd frontend
npm run dev
```

## Создание новых данных

### Создать транзакцию

1. Перейдите на страницу транзакций
2. Нажмите "Создать транзакцию"
3. Заполните форму:
   - Тип: доход/расход
   - Сумма: любое число
   - Категория: выберите из списка
   - Описание: опционально
   - Дата: выберите дату
4. Нажмите "Сохранить"
5. Транзакция должна появиться в списке
6. Дашборд должен обновиться

### Создать категорию

1. Перейдите на страницу категорий
2. Нажмите "Создать категорию"
3. Заполните форму:
   - Название: любое
   - Иконка: эмодзи
   - Цвет: выберите цвет
   - Тип: доход/расход
4. Нажмите "Сохранить"
5. Категория должна появиться в списке

### Создать бюджет

1. Перейдите на страницу бюджетов
2. Нажмите "Создать бюджет"
3. Заполните форму:
   - Категория: выберите из списка
   - Сумма: любое число
   - Период: месяц/год
   - Даты: начало и конец
4. Нажмите "Сохранить"
5. Бюджет должен появиться в списке

## Известные проблемы

### 1. Кодировка в PowerShell
- Русские символы могут отображаться некорректно в curl
- Это не влияет на работу API
- В браузере все отображается корректно

### 2. Fast Refresh warnings
- Next.js может показывать предупреждения о Fast Refresh
- Это нормально при разработке
- Страница автоматически перезагружается

## Следующие шаги

После успешного тестирования:

1. ✅ Проверить все страницы
2. ✅ Создать несколько транзакций
3. ✅ Проверить фильтрацию
4. ✅ Проверить пагинацию
5. ✅ Проверить графики и аналитику
6. ✅ Проверить экспорт/импорт
7. ✅ Запустить тесты: `npm test`
8. ✅ Сделать коммит изменений

## Полезные команды

```bash
# Проверка типов
cd frontend && npm run type-check

# Линтинг
cd frontend && npm run lint

# Форматирование
cd frontend && npm run format

# Тесты
cd frontend && npm test

# Проверка бэкенда
cd backend && pytest

# Проверка API
node test_api_format.js
node test_integration.js
```

## Контакты для помощи

Если возникли проблемы:
1. Проверьте консоль браузера
2. Проверьте логи бэкенда
3. Проверьте Network tab в DevTools
4. Создайте issue с описанием проблемы и скриншотами
