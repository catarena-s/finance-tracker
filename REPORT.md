# Отчёт о разработке Finance Tracker

## Обзор

Этот документ содержит историю разработки проекта Full-Stack трекера личных финансов.

## 14.02.2027(≈8 часов)
1)  Создание спецификации 
2)  Настройка инфраструктуры(создание основных директорий, настройка  Git конфигурации, документацию проекта ARCHITECTURE и README, Docker Compose, CI/CD pipeline)
3)  Реализация и отладка Backend API

Почему-то не удалось сделать номальный промпт через ChatGpt - он услиненно урезал требование к функционалу и не только🙁. 
Так и не смогла понять кто из нас затупил он или я. В результате пришлось допиливать его вариант руками. 

Для работы решила выбрать __Kiro AI Ide__. Раньше с ним не работала. На одном из уроков его показал преподователь, заинтересовал режим Spec. Решила попробовать. Пока под впечатлением от его возможностей.

Сладывается ощущение, что Kiro делает даже больше чем нужно, но решила его не сильно граничивать, интересно, что получится в результате.

Kiro периодически путается и смешивает русский и английский языки, там где критично для документации приходится просить перегенерить.

Решила добавить дополнительный файл CHANGELOG.md - для фиксации изменений.

Kiro усиленно пытался забыть закомитить шаг с созданием спецификаций и правил для IDE, пришлось напомнить несколько раз. Это можно было сделать в ручную, но хотелось всё же что бы настроился автоматический процесс создания новых веток и комит изменение. 
Создание веток ещё в процессе отладки, а с автоматической подготовкой коммитов(без напоминаний) поцесс удалось наладить. 

__Итого__:
- Cоздание спецификаций, дизайн и файла с задачами(генерация была в полуавтоматическом режиме, периодически запрашивал подтверждения) ушло около 2х часов.
- Kiro попытался сделать лишнюю работу: создать спецификацию core-features, которая дублировала спеуификации фронтенда и бэкенда. Вовремя заметила и остановила его.
- Генерация основной документации и создании структуры проекта по плану ≈1.5 часа.
- Генерация backend с проверкой выполенния тестов ≈ 1.5 часа(здесь видимо Kiro устал, стал чаще тупить и подвисать, приходилось повторять команды 🙁)
- Около 2х часов(на 2 PR) проверка и настройка CI/CD pipeline и отладка запуска Docker Compose(1й PR)

## 15.02.2027(≈4,5 часа)
- Реализация и отладка Frontend 

Обнаружила, что на этапе Backend потерялась задача на создание БД(она не была внесена в задачи)🤯, хотя задача на создание скриптов миграций была, и они даже были созданны.

Kiro пытается халтурить, или возможно иногда тупит(кто его знает) - отметил некоторые задачи как выполненные, а пофакту их не сделал(нужно будет перепроверить бэкенд часть😤). Обнаружилось это когда попросила перепроверить, что он реально сделал, а что нет.

При создании списка задач часть здач на создание тестов пометил как не обязательнуы и осознанно пропустил их при выполнение. Обнаружилось это когда начала в ручную тустить фронт, а фронт не работает 😒. После написания интеграционных тестов ошибки ушли.
Подозреваю с бэкендом таже история...

Пришлось создавать дополнительные файлы с правиласт для комитов и PR, постоянно сбивался на английский язык и пытался написать роман в 3х томах в качестве комментария для PR.

## 19.02.2027(≈5 часа)
- Доработка Backend(создание бд, написание интеграционных тестов, сквозное тестирование, исправление ошибок)
- Исправление Frontend(исправление форматов, дополниетельные тесты)

Как и предполагала Kiro посчитал, что интеграционные тесты - это не важно и все их пропустил. 

Простого написания интеграционных тестов оказалось недостаточно, потому что он не посчитал нужным обеспечить их успешное выполнение 😤.
И если за ним не проследить, он рапортует, что всё сделал и всё хорошо — можно коммитить, а потом ловить ошибки в CI/CD из-за падающих тестов.

Хорошо помог в отладке кода запуск сквозных тестов.

При достижении лимита контекста создаётся новый чат, и Kiro иногда забывает не только контекст, но и о том, что нужно обращаться к собственным правилам - это отдельная печаль и как её побороть пока не поняла :(

Тестов много не бывает. Если кажется, что их много, то их всё равно мало!
