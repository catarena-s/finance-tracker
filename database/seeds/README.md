# Seed данные для базы данных

Этот каталог содержит SQL скрипты для заполнения базы данных начальными данными.

## Файлы

- `seed_categories.sql` - 12 категорий (3 доходы + 9 расходы)
- `seed_budgets.sql` - 3 бюджета для разных категорий
- `seed_transactions.sql` - 220 транзакций за последние 6 месяцев
- `generate_transactions.py` - Python скрипт для генерации транзакций

## Использование

### Автоматическая загрузка (через Docker)

При запуске через Docker Compose seed данные загружаются автоматически:

```bash
docker-compose up -d
```

### Ручная загрузка

Если нужно перегенерировать транзакции:

```bash
cd database/seeds
python generate_transactions.py
```

Затем загрузить в БД:

```bash
psql -h localhost -p 5433 -U postgres -d finance_tracker < seed_categories.sql
psql -h localhost -p 5433 -U postgres -d finance_tracker < seed_budgets.sql
psql -h localhost -p 5433 -U postgres -d finance_tracker < seed_transactions.sql
```

## Структура данных

### Категории (12 шт)

**Доходы:**
- Зарплата 💰
- Фриланс 💼
- Инвестиции 📈

**Расходы:**
- Продукты 🛒
- Транспорт 🚗
- Жильё 🏠
- Развлечения 🎬
- Здоровье ⚕️
- Образование 📚
- Одежда 👔
- Кафе и рестораны 🍽️
- Прочее 📦

### Бюджеты (3 шт)

- Продукты: 15,000 ₽/месяц
- Развлечения: 10,000 ₽/месяц
- Транспорт: 5,000 ₽/месяц

### Транзакции (220 шт)

- **Период:** последние 6 месяцев
- **Распределение:** ~30-40 транзакций/месяц
- **Доходы:** 15% (33 транзакции)
- **Расходы:** 85% (187 транзакций)
- **Валюта:** RUB (российский рубль)

**Реалистичные суммы:**
- Зарплата: 70,000-80,000 ₽
- Фриланс: 15,000-35,000 ₽
- Аренда: 20,000-30,000 ₽
- Продукты: 500-3,000 ₽
- Транспорт: 200-2,000 ₽
- И т.д.

## Генерация новых данных

Скрипт `generate_transactions.py` позволяет настроить:

- Количество транзакций (по умолчанию 220)
- Период в месяцах (по умолчанию 6)
- Соотношение доходов/расходов (15%/85%)
- Диапазоны сумм для каждой категории

Для изменения параметров отредактируйте константы в начале файла:

```python
# Количество транзакций
transactions = generate_transactions(count=220, months=6)

# Категории и диапазоны сумм
INCOME_CATEGORIES = [
    ('id', 'Название', (min_amount, max_amount)),
    ...
]
```

## Примечания

- Все ID категорий фиксированные (UUID) для консистентности
- Транзакции сортируются по дате (старые первыми)
- Зарплата всегда 15-го числа месяца
- Аренда всегда 1-го числа месяца
- Описания транзакций реалистичные и разнообразные
